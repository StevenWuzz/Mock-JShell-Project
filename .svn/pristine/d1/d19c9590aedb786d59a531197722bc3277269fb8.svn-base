Class name: StandardInput
Parent class (if any): None
Subclasses (if any): None 

Responsibilities:
* Parses user input
* Sends input to ErrorMessage to check if input is not valid
* Sends input to Commands if input is valid

Collaborators:
* Commands
* ErrorMessage
