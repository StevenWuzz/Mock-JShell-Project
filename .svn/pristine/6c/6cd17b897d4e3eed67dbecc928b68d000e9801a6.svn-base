package commands;

import fileSystem.*;

public class List{ // TODO: extends Command later

	
	/**
	 * Execute the Ls command
	 * 
	 * @param parameter
	 */
	public void execute(String[] parameter)
	{
		//need the StandardInput implemented first
		return;
	}
	
	/**
	 * Check whether the given path is a directory or a file
	 * 
	 * @param path
	 * @return a string specifying whether the path is a directory or a file
	 */
	public String checkDirOrFile(String path) {
		
		// Traverse and then use instanceOf
		return null;
	}
	
	
	/**
	 * Recursively traverse the given path if the path is a directory
	 * 
	 * @param path
	 */
	public void traverseDir(String path) {
		
		/* Assuming that the given path is a full path */
		/*
		String[] dirList = path.split("/",2);
		String firstDirName = dirList[0];
		
		Directory currentDir = FileDatabase.getRoot();
		if(currentDir.getName() != firstDirName) {
			currentDir = FileDatabase.getRoot();
			
		}*/
		
		//Alternative method: Obtain a full path for any kind of path, and use that
		
		
	}
	
	/**
	 * Print all the contents in a given directory
	 */
	public void printContentsInDirectory() {
	  
	  //Traverse first, call the traverseDir method
	  
	  //Assuming we already get to the directory
	  Directory dir; // = ....
	  int numOfChildren = dir.getNumOfDirectoryChildren();
	  for(int i = 0; i < numOfChildren; i++) {
	    // Need access to dirChildren
	    // Print each children
	  }
	}
}
